A simple backend Typescript Express server that only exposes one end point.

Sample data is stored in "/data/sample-fund-data.csv" and proccessed in "src/routes/default.ts" .

To install dependecies you must be in the "/backend" folder directory.

After, run `npm install` and then run `npm run start` to start the server.
